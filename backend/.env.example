# ===== existing =====
GROQ_API_KEY=
ELEVENLABS_API_KEY=
EMAIL_USER=dabushadir97@gmail.com
EMAIL_PASS=                     # Gmail App Password (16-char), NOT the main password
EMAIL_FROM_NAME=Adir Dabush

# ===== job agent =====
DATABASE_URL=                   # postgres://... (Supabase / Neon free tier)
TELEGRAM_BOT_TOKEN=             # from @BotFather
TELEGRAM_CHAT_ID=               # your personal chat id (use @userinfobot to find it)
TELEGRAM_WEBHOOK_SECRET=        # any random string — set on setWebhook call
AGENT_CRON_TOKEN=               # `openssl rand -hex 32`
RELEVANCE_THRESHOLD=70          # 0-100, scores >= this get Telegram approval
DAILY_EMAIL_CAP=15              # max outbound emails per day
GROQ_CALL_DELAY_MS=2500         # delay between Groq calls (free tier rate limit)
